# BSB AI Lessons

Teaching materials for AI-assisted development on the **BUSY Bar**. Content is split into two top-level folders:

| Folder | Purpose |
|--------|---------|
| **[Official/](Official/)** | **Standard product path:** HTTP **widgets** (Python + `POST /api/display/draw`, 72×16 strip), **openapi.yaml**, **CLI/**. |
| **[Unofficial/](Unofficial/)** | **Non-default / optional:** **JerryScript** on-device apps, **Lua** Custom APPS (`busy.*`), experiments, snapshots — not assumed on every stock firmware. |

## Quick links

- **Widgets (host PC → device):** [Official/README.md](Official/README.md) — lessons `01`–`11`, `openapi.yaml`, `CLI/`.
- **JerryScript apps (code on device):** [Unofficial/12-JerryScript-on-device-apps.md](Unofficial/12-JerryScript-on-device-apps.md).
-- **Lua on device (historical):** [Unofficial/Custom-APPS-Lua/README-Custom-APPS.md](Unofficial/Custom-APPS-Lua/README-Custom-APPS.md).

This folder is tracked inside the main `BSB-Custom-Widgets` repo (no separate AI-lessons repo).

## Single full document

For a one-shot prompt, the full **Knowledge Sheet** in the parent BSB-Custom-Widgets tree (if present) remains an alternative; these lesson files are for topic-by-topic or MCP use.
